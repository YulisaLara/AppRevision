<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Configuración de Cuenta - Matemágicos</title>
    <!-- Agregar los enlaces a los archivos CSS y JavaScript de Bootstrap -->
    <script src="../resources/js/delete-account.js"></script>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <!-- Estilo CSS personalizado -->
    <link rel="stylesheet" href="../resources/css/dashboard.css">
    <style>
        /* Estilo CSS personalizado para el contenido */
        .content-card {
            max-width: 400px;
            margin: 0 auto;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .confirmation-header {
            font-size: 24px;
            margin-bottom: 20px;
        }

        .confirmation-text {
            font-size: 16px;
            margin-bottom: 20px;
        }

        .btn-container {
            display: flex;
            justify-content: space-between;
        }

        #confirmButton {
            flex: 1;
            margin-right: 10px;
        }

        #cancelBtn {
            flex: 1;
        }
    </style>
</head>

<body>
    <!-- Barra de navegación -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/dashboard"><img src="../resources/img/logo.svg" alt="Logo"></a>
        <!-- Resto del contenido de la barra de navegación -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="px-3 btn btn-danger" type="button" href="logout">Cerrar Sesión</a>
                </li>
            </ul>
        </div>
    </nav>
    <div class="content-card">
        <div class="confirmation-header">Confirmar Eliminación de Cuenta</div>
        <div class="confirmation-text">¿Estás seguro de que deseas eliminar tu cuenta? Esta acción no se puede deshacer.
        </div>
        <form action="/delete-account" method="DELETE">
            <div class="btn-container">
                <button type="submit" class="btn btn-danger" id="confirmButton">Eliminar</button>
                <a class="btn btn-primary" id="cancelBtn" href="/settings">Cancelar</a>
            </div>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <% if(alert) { %>
        <script>
            Swal.fire({
                title: '<%= alertTitle %>',
                text: '<%= alertMessage %>',
                icon: '<%= alertIcon %>',
                showConfirmButton: <%= showConfirmButton %>,
                timer: <%= timer %>
      }).then(() => {
                    window.location = '/<%= ruta %>'
                })
        </script>

        <% }%>


            <div id="alertContainer" class="fixed-top w-100 p-3 text-center"></div>

            <!-- Pie de página -->
            <footer class="footer">
                <div class="container">
                    <p>&copy; 2023 Matemágicos</p>
                </div>
            </footer>

</html>